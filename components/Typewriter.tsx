"use client";

import React from "react";
import TypewriterComponent, { TypewriterState } from "typewriter-effect";

const Typewriter: React.FC<any> = (props) => {
    return <TypewriterComponent {...props} />;
};

export default Typewriter;
